# MTC Verifiable Index Verifier (`mtcverify`)

`mtcverify` queries and cryptographically verifies domain lookups in an MTC verifiable index.

## Usage

```bash
go run ./vindex/v1/cmd/mtcverify/ \
  --vindex_url=http://localhost:8088 \
  --domain=example.com
```
